<!DOCTYPE html>
<html lang="en" class="bg-gray-100">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Slack Course – Settings</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="min-h-screen flex items-center justify-center px-4 overflow-x-auto">

    <body class="min-h-screen flex flex-col items-center justify-center px-4 overflow-x-auto relative">

        <!-- Back + Flash messages -->
        <div class="absolute top-4 left-4 right-4 flex flex-wrap items-start gap-4 z-50">

            <!-- Back button -->
            <a href="/selector" class="bg-gray-200 hover:bg-gray-300 text-gray-800 px-4 py-2 rounded shadow-sm transition">
                ← Back
            </a>

            <!-- Flash messages -->
            <% if (error) { %>
            <div class="px-4 py-2 rounded bg-red-100 border border-red-400 text-red-700">
                <span class="block sm:inline"><%= error %></span>
            </div>
            <% } %>

            <% if (success) { %>
            <div class="px-4 py-2 rounded bg-green-100 border border-green-400 text-green-700">
                <span class="block sm:inline"><%= success %></span>
            </div>
            <% } %>
        </div>

        <div class="w-full max-w-[1200px] bg-white rounded-2xl shadow-xl p-6 space-y-6">

            <h1 class="text-2xl font-bold text-gray-800 mb-4">Account Settings</h1>

            <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
                <!-- Password Change Form -->
                <form action="/settings/password_change" method="post" class="space-y-3 bg-gray-50 p-4 rounded-xl shadow-sm">
                    <h2 class="text-lg font-semibold text-gray-700">Change Password</h2>
                    <div>
                        <label for="old_passwd" class="block text-sm font-medium text-gray-600">Old Password</label>
                        <input type="password" name="old_passwd" id="old_passwd" class="w-full mt-1 px-3 py-2 border rounded-lg text-sm" required>
                    </div>
                    <div>
                        <label for="new_passwd" class="block text-sm font-medium text-gray-600">New Password</label>
                        <input type="password" name="new_passwd" id="new_passwd" class="w-full mt-1 px-3 py-2 border rounded-lg text-sm" required>
                    </div>
                    <button type="submit" class="bg-blue-600 text-white text-sm px-3 py-2 rounded-lg hover:bg-blue-700 transition">Update</button>
                </form>

                <!-- Username Change Form -->
                <form action="/settings/username_change" method="post" class="space-y-3 bg-gray-50 p-4 rounded-xl shadow-sm">
                    <h2 class="text-lg font-semibold text-gray-700">Change Username</h2>
                    <div>
                        <label for="new_username" class="block text-sm font-medium text-gray-600">New Username</label>
                        <input type="text" name="new_username" id="new_username" class="w-full mt-1 px-3 py-2 border rounded-lg text-sm" required>
                    </div>
                    <div>
                        <label for="passwd" class="block text-sm font-medium text-gray-600">Password</label>
                        <input type="password" name="passwd" id="passwd" class="w-full mt-1 px-3 py-2 border rounded-lg text-sm" required>
                    </div>
                    <button type="submit" class="bg-blue-600 text-white text-sm px-3 py-2 rounded-lg hover:bg-blue-700 transition">Update</button>
                </form>

                <!-- Email Change Form -->
                <form action="/settings/email_change" method="post" class="space-y-3 bg-gray-50 p-4 rounded-xl shadow-sm">
                    <h2 class="text-lg font-semibold text-gray-700">Change Email</h2>
                    <div>
                        <label for="new_mail" class="block text-sm font-medium text-gray-600">New Email</label>
                        <input type="email" name="new_mail" id="new_mail" class="w-full mt-1 px-3 py-2 border rounded-lg text-sm" required>
                    </div>
                    <div>
                        <label for="passwd0-email" class="block text-sm font-medium text-gray-600">Password</label>
                        <input type="password" name="passwd" id="passwd0-email" class="w-full mt-1 px-3 py-2 border rounded-lg text-sm" required>
                    </div>
                    <button type="submit" class="bg-blue-600 text-white text-sm px-3 py-2 rounded-lg hover:bg-blue-700 transition">Update</button>
                </form>
            </div>
        </div>
    </body>

</html>